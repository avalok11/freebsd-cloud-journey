# Шеллы: разделение ролей — tcsh для интерактива, bash+Python для скриптов

**Время чтения:** ~4–6 минут
**Фаза:** 0 (вводная теория перед практикой)
**Цель:** раз и навсегда разделить «где я живу» и «где я работаю», чтобы потом не страдать.

---

## 1. Два мира: интерактивный vs скриптовый

**Интерактивный шелл** — то, что видишь, когда открываешь терминал и вводишь команды.
**Скриптовый язык** — то, на чём пишешь автоматизацию, которая выполняется без твоего участия.

Это **разные задачи** с разными требованиями. И для каждой свой инструмент.

## 2. tcsh/csh — наш интерактивный шелл

**Почему tcsh/csh исторически нативен для FreeBSD:**

- До FreeBSD 13.x `csh` был дефолтным шеллом для root (потом поменяли на sh, но традиция осталась).
- В `Absolute FreeBSD» Лукас пишет все примеры на csh — это «книга для админа FreeBSD».
- В Handbook FreeBSD многие секции про интерактивную работу даны на csh.
- Синтаксис для интерактива **удобный**: history-механизм, автодополнение, control-A/E для навигации по строке.

**Что tcsh умеет хорошо:**

- `set foo = bar` — присваивание.
- `if ( -f /etc/passwd ) then ... endif` — управляющие конструкции.
- `foreach i ( *.txt )` — цикл по списку.
- `setenv PATH /usr/local/bin:$PATH` — переменные окружения.
- Встроенный history с поиском по Ctrl-R.

**Что tcsh плохо:**

- Обработка пайпов с ошибками — не POSIX-совместимо.
- Массивы — неудобно.
- Functions (функции) — слабо.
- Совместимость с другими скриптами — никакая.
- **Главное:** ни в одной документации, ни в одном примере из мира автоматизации ты не встретишь tcsh.

## 3. bash (POSIX sh) — наш скриптовый шелл

**Почему bash/POSIX sh:**

- **POSIX-совместимо.** Весь мир автоматизации: Ansible, Bastille, vm-bhyve, FreeBSD Handbook, все готовые скрипты — POSIX sh/bash.
- **Читается в любой доке.** Любой туториал на serverfault, в Handbook, в readme на GitHub — на bash.
- **Идемпотентность.** Bash-скрипт при правильном написании можно запускать много раз, результат одинаков.
- **Совместимость с Ansible.** Ansible использует POSIX sh для подключения к управляемым нодам.

**Что bash умеет хорошо:**

- `if [ -f /etc/passwd ]; then ... fi` — POSIX-стиль.
- `for i in *.txt; do ... done` — цикл.
- `set -euo pipefail` — строгая обработка ошибок.
- Массивы: `arr=(a b c)`, `${arr[0]}`.
- Functions: `my_func() { ... }`.
- Подстановки: `$(command)`, `${var}`. 

**Shebang для скриптов:**

```bash
#!/bin/sh           # POSIX sh, максимально совместимо
#!/usr/bin/env bash # bash-специфичные фичи
```

## 4. Python — для сложной логики

**Когда Python вместо bash:**

- Логика больше 20 строк.
- Работа с JSON, YAML, API.
- Циклы с нетривиальной обработкой ошибок.
- Парсинг логов, метрик.
- Сетевые вызовы, библиотеки.

**Примеры из нашего проекта:**

- Парсинг JSON-ответа от Ansible в скрипте деплоя.
- Анализ метрик Prometheus для алертинга.
- Скрипт, который собирает данные с нескольких серверов и формирует отчёт.
- Интеграция с Telegram API для уведомлений.

**Почему Python:**

- Ты его уже знаешь (ООП, работал с ОФД-интеграцией).
- Стандартная библиотека + pip = 90% задач.
- Читается легко, поддерживается через 5 лет без боли.

**Shebang:**

```python
#!/usr/bin/env python3
```

## 5. Почему НЕ tcsh в скриптах

**Конкретный пример — установка пакета с проверкой:**

**bash (POSIX-совместимо, читается везде):**
```bash
#!/bin/sh
if ! pkg info -q nginx > /dev/null 2>&1; then
    logger "nginx not installed, installing"
    pkg install -y nginx
    sysrc nginx_enable=YES
    service nginx start
fi
```

**tcsh (нигде в продакшене так не пишут):**
```tcsh
#!/bin/tcsh
set pkg_check = `pkg info -q nginx`
if ( $? != 0 ) then
    logger "nginx not installed, installing"
    pkg install -y nginx
    sysrc nginx_enable=YES
    service nginx start
endif
```

Видишь: bash-вариант ты увидишь в **любом** учебнике, **любой** официальной доке, **любом** скрипте на GitHub. tcsh-вариант — только в музее.

**Реальные проблемы с tcsh в скриптах:**

1. **Ansible.** Модули ожидают POSIX sh на управляемой ноде. tcsh ломает пайпы, обработку stderr.
2. **Bastille, vm-bhyve.** Все management-скрипты на bash.
3. **Переносимость.** Скрипт на tcsh работает только на FreeBSD. Скрипт на bash — везде.
4. **Читаемость.** Коллега (или ты через год) посмотрит на tcsh-скрипт и потратит время на расшифровку.
5. **Onboarding.** Если в команде появится Linux-админ, tcsh его остановит.

## 6. Когда tcsh всё-таки уместен

**Только в интерактиве.** Открыл терминал, набираешь команды — tcsh. Не пишешь скрипты.

**В нашем проекте:**

- Login shell на FreeBSD — `csh` или `tcsh` (как тебе привычнее).
- Любой скрипт в `infra-configs`, `bastille`, ansible — на bash/POSIX sh.
- Скрипты сложнее 20 строк — на Python.

**Это компромисс, который работает:**

- Твоя зона комфорта в интерактиве остаётся.
- Скрипты пишутся в стандартной экосистеме.
- Никаких сюрпризов с Ansible.

## 7. Настройка по умолчанию в нашем стенде

**На FreeBSD-нодах (всё через Ansible):**

```yaml
# /etc/rc.conf
defaultrouter="..."

# /etc/passwd
alexey:*:1000:1000:Alexey:/home/alexey:/bin/tcsh
```

**В скриптах автоматизации (Ansible-роли, Bastillefile, мониторинг):**

```bash
#!/bin/sh
# или
#!/usr/bin/env bash
```

**Скрипты обработки данных:**

```python
#!/usr/bin/env python3
```

## 8. Что НЕ делать

**Не надо:**

- Писать `#!/bin/tcsh` в скриптах. Вообще никогда. Точка.
- Использовать `csh` для cron-задач. Только sh/bash.
- Спорить в интернете про csh vs bash. Это как vim vs emacs — бесполезно.
- Переписывать существующие bash-скрипты на tcsh «потому что FreeBSD».

**Надо:**

- tcsh для интерактива — твоя зона.
- bash/POSIX sh для скриптов автоматизации — стандарт.
- Python для сложной логики — твой навык.
- **Shebang в скриптах ВСЕГДА `#!/bin/sh` или `#!/usr/bin/env bash` или `#!/usr/bin/env python3`**. Никогда `#!/bin/tcsh`.

## Что почитать

- «Classic Shell Scripting» (Robbins, Beebe) — фундаментальная книга.
- «Efficient Linux at the Command Line» (Jansen) — современная практика.
- FreeBSD Handbook, Chapter 3 (Unix Basics) — если нужно освежить базу.
- Python docs: https://docs.python.org/3/ — справочник, не читать подряд.

---

**Вопрос для самопроверки:** тебе нужно написать скрипт, который каждую ночь:
1. Делает ZFS snapshot всех jail-ов.
2. Отправляет snapshot на удалённую ноду через `zfs send | ssh ...`.
3. Удаляет snapshot старше 7 дней.
4. Пишет результат в syslog.
5. В случае ошибки — отправляет сообщение в Telegram.

На bash это 30 строк. На tcsh — 60 строк с нечитаемым синтаксисом. На Python — 40 строк с try/except и нормальной обработкой ошибок. **Какой вариант выберешь и почему?**
